export interface DialogModalRoleData {
  role: string;  
  status_role: boolean;
  created_at: Date;
  updated_at: Date;
}

export interface Name {
  name: string;
  first: string;
  last: string;
}

